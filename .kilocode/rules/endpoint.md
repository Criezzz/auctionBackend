# endpoint.md

Rule that you need to follow

## Guidelines
- File API_SYNC là từ bên frontend đưa ra instruction với backend
- Luôn cập nhật lại API_ENDPOINTS_GUIDE.md mỗi khi có chỉnh sửa liên quan tới api endpoint để truyền lại file này cho agent ở frontend
- Không dùng emoji khi comment nếu có
- Nếu tạo script test thì cần xóa bỏ sau khi test thành công
